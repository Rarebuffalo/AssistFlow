{
  "name": "spurdesk",
  "version": "1.0.0",
  "description": "SpurDesk customer support platform with AI agent",
  "scripts": {
    "install:all": "npm install --prefix spurdesk-api && npm install --prefix spurdesk-web",
    "dev:api": "npm run dev --prefix spurdesk-api",
    "dev:web": "npm run dev --prefix spurdesk-web",
    "dev": "npx -y concurrently \"npm run dev:api\" \"npm run dev:web\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
